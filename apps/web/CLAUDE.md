# CLAUDE.md — apps/web

Working app: upstream orbat-mapper code synced via `scripts/sync-upstream.py` plus our custom modules (auth, API client, symbol editor).

Custom files are listed in `scripts/custom-files.txt` and are protected from sync overwrites.
